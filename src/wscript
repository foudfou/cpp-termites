# dear emacs, this is -*- python -*-
import waflib.Logs as msg

def configure(cnf):
    msg.info("configure [src]...")

def build(bld):
    msg.info("build [src]...")
    bld.program(
        features = 'cxx cxxprogram',
        source   = bld.path.ant_glob('*.cpp'),
        target   = 'termites',
        includes = '../include .',
    )
